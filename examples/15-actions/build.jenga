
*run : the.sh
  ./$<

*test : the.out the.out.expected
  diff $^

the.out : the.sh
  ./$< > $@

promotion.rule :
  if [ ! -z "$promote" ]; then echo '!the.out.expected : the.out : ln $$< $$@'; fi > $@

include promotion.rule
