
*view: report
  cat report

report: ball.tar
  echo -------------------- >> $out
  tar -tf ball.tar | sort >> $out
  echo -------------------- >> $out

ball.tar: my.all @my.all
  tar -cf $out $$(cat my.all)

my.all :
  echo '$glob:.' > $out
