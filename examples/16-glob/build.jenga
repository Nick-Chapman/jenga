
*view: report
  cat report

report: ball.tar
  echo -------------------- >> $@
  tar -tf ball.tar | sort >> $@
  echo -------------------- >> $@

ball.tar: my.all @my.all
  tar -cf $@ $(cat my.all)

my.all :
  echo '$glob:.' > $@
