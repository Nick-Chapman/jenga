
expect.names  :
  echo '$glob' | grep -v ^rules.sh$$ | grep \.sh$$ | sed 's|\.sh$$||' | sort > $@

include ../rulelib/expect.jenga

self-test.deps : : echo 'jenga ../src/jenga.exe' > $@

rules : rules.sh expect.names
  ./rules.sh $$(cat expect.names) > $@

include rules
